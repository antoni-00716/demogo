# 2026-06-12

## 首页跳过的全量 iOS 设计改版
基于架构师差距分析报告，实施除首页外的所有页面 iOS 设计改版。

### Dashboard 侧改动
- **ProjectsView**: DemoList 改为 `.project-grid` 卡片网格布局
- **UploadPanel**: DropZone 改为 dashed border iOS 样式，添加 form-label/form-input/form-select
- **AgentPublishPanel**: 添加 AI 提示词卡片（ai-textarea、生成并发布按钮）+ 最近作品列表
- **FeedbackCollection**: 新建组件，实现筛选按钮 + 反馈卡片（badge 类型标签、unread 绿边框）
- **PlanView**: 重构为 plan-grid 套餐卡 + usage-bar 用量进度条
- **DeployHistory**: 添加 status-badge 状态标签

### Admin 侧改动
- **AdminUsers/AdminDemosView/AdminFeedback**: 添加 stats-grid 统计行 + 筛选按钮
- **AdminContentReviews**: 添加筛选按钮 + risk-tag
- **AdminForms**: 改为 forms-grid 卡片网格布局

### CSS
- dashboard.css 新增 iOS 子页面样式全集（~400 行 CSS）

### 验证
- tsc 0 errors, vite build 346ms

### 未实施
- AdminAnalytics（新页面）、PreviewPage（新页面）、LoginPage tabs—低优先级
- 首页跳过

### 部署
- dist/demogo-site-preview.zip 已打包

## iOS T5/T6 CSS+JSX 重构完成

**提交**：`244e28c` — 25 files, +725/-281

**目标**：将 T5（用户工作台）/ T6（管理后台）的 JSX 类名对齐到 iOS 极简风格 CSS 体系

**完成项**：
1. **dashboard.css 补充** — 新增 598 行 iOS 极简类名（section-mini-head, hosting-architecture, risk-panel, 等）
2. **WorkspaceHero → OverviewView 合并** — welcome greeting + onboarding banner 已嵌入 OverviewView，删除 WorkspaceHero.tsx
3. **DemoList.tsx 重构** — project-item-* → demo-* 系列类名
4. **ProjectsView.tsx 重构** — projects-split → view-stack
5. **Admin 文件** — 所有 request-main → panel
6. **验证** — tsc 0 errors, vite build 236ms

**注意**：工程师代理最多跑了 80 轮后超时（任务量太大），后续手动接手了提交和最终验证。

## Refactor: Extract services from server.js

**问题**：server.js 有大量内联函数，导致文件膨胀（~1086 行），逻辑分散。

**完成的工作**：

1. **Upload middleware 接管** (`middleware/upload.js`)
   - 移除 server.js 中内联的 multer 配置（~390-417 行）
   - 替换为 `createUploadMiddleware({ maxZipSizeMb, uploadDir, isSupportedArchiveName })`

2. **Auth helpers 提取** (`lib/auth-helpers.js`)
   - 新建模块，`createAuthHelpers({ sessionsFile, usersFile })` factory 模式
   - 提取 `readBearerToken`、`getUserFromRequest`、`getUserFromAgentToken`
   - 移除 server.js 底部 4 个函数定义（lines ~894-940）

3. **Deploy handler 接管** (`services/deploy-handler-service.js`)
   - 导入并使用已存在的 `createDeployHandlerService`
   - 内联的 `handleCreateDeployment` / `handleUpdateDeployment`（~100 行）替换为服务版本
   - 服务版本额外包含 **preInspection 前置检查**、**队列可用性判断**、**recordDeployFailure 统一错误处理**
   - 将 `resolveAgentUpdateDemoId` 前移至 deploy-handler 初始化之前

4. **测试覆盖扩展**
   - `admin-routes.test.mjs` — 新增 7 个测试，覆盖 filterAdminDemos 的状态过滤、搜索
   - `pipeline-service.test.mjs` — 新增 3 个测试，验证 factory API 和纯函数
   - 测试总数: **176 → 186** (+10) ✅

**验证**：
- `node --check` 语法检查 ✅ 所有涉及文件通过
- `186 tests pass, 0 fail` ✅
- 重构上线后服务健康检查通过 ✅
- Commits: `12c369b` (重构), `edde5ef` (测试)

## 完成 13 个 iOS 设计系统 HTML 页面
- 基于 `demogo-dashboard-ios.html` / `demogo-admin-ios.html` 两个基础框架
- **Dashboard 6 页**: 我的作品、上传发布、AI 发布、发布记录、反馈收集、套餐额度
- **Admin 7 页**: 用户管理、Demo 管理、内容审核、反馈管理、表单管理、数据分析、系统设置
- 全中文、共享 CSS 变量体系、一致的侧边栏和响应式断点
- 文件位于 `deliverables/`，新增同目录 `all-ios-pages-overview.md` 总览文档

## iOS 子页面样式实施（基于 22 个 HTML 原型）

**dashboard.css 新增**：Dashboard + Admin 全部 13 个子页面的 iOS 样式
- Dashboard: .project-grid, .plan-grid, .usage-bar-group, .feedback-filters, .search-box-pill, .upload-card, .ai-card, .status-badge, .table-btn, .btn-pill--outline/--disabled
- Admin: .review-card, .analytics-grid, .forms-grid, .settings-card, .toggle-switch, .filter-select, .risk-tag, .type-tag, .btn-approve/reject/save, .td-* 系列
- 新增 ~500 行 CSS，对齐 deliverable/ 下所有 iOS HTML 原型

**React 组件更新**：
- ProjectsView.tsx: 从 DemoList 行列表 → iOS 卡片网格 + pill 搜索框（useState 搜索过滤）
- FeedbackPanel.tsx: 新增 iOS feedback-filters 筛选器

**验证**：tsc 0 errors, vite build 273ms ✅

**注意**：AgentPublishPanel / UserDashboard PlanView 已由前置作业更新为 iOS 类名，Admin 子页面 React 组件通过 CSS 自动受益无需改动。

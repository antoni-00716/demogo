# 2026-06-11 iOS 极简风前端改版

## 概述
将 DemoGo 前端从青色调（#06B6D4）的复杂渐变色风格，全面改版为翠绿色（#22C55E）iOS 极简风（Apple 风格）。

## 任务分解
架构师（software-architect）完成差距分析，产出 `docs/ios-redesign-plan.md`，将工作分解为 6 个任务（T1-T6）。

## 实施清单（工程师 software-engineer 完成）

### T1: CSS Design Tokens 更新
- `src/styles/design-tokens.css` — 品牌色 #06B6D4→#22C55E，新增 Apple 阴影/圆角/动效 tokens

### T2: 全局样式与组件库 iOS 化
- `src/styles/global.css` — 导航 64px→48px，按钮 pill 化，卡片简化
- `src/components/BrandLogo.tsx` / `Button.tsx` / `Card.tsx` / `Badge.tsx` / `Toast.tsx` / `MetricCard.tsx` — 全部通过 CSS 变量自动适配

### T3: 首页改版
- `src/pages/HomePage.tsx` — JSX 结构调整为 iOS 布局（文案零改动）
- `src/styles/home.css` — 全面重写

### T4: 登录页改版
- `src/styles/auth.css` — 纯色背景，卡片简化
- `LoginPage.tsx` — 通过 CSS 自动适配

### T5: 用户工作台改版
- `src/pages/dashboard/Sidebar.tsx` — emoji 图标替代 lucide-react
- `src/styles/dashboard.css` — 全面重写

### T6: 管理后台改版
- `src/pages/admin/AdminSidebar.tsx` — emoji 图标
- dashboard.css 共享样式已覆盖

## 修复
- `src/components/dashboard/DemoList.tsx` — 硬编码 cyan 渐变 → green

## 验证
- ✅ `npx tsc --noEmit` — 0 类型错误
- ✅ `npx vite build` — 1.41s 构建成功，1805 modules
- ✅ 无 `#06B6D4` / `cyan` / `electric` / `indigo` / `violet` 残留
- ✅ 文案零改动

## 成果文件
共修改 17 个源文件，全部改动均为纯 CSS 驱动（仅 Sidebar 组件需手动更换 emoji 图标），零新增依赖包。

## 部署到服务器
1. 打包 `web/dist/` → `dist/demogo-site-preview.zip` (118KB)
2. SCP 上传到 `root@demogo.cn:/tmp/`
3. SSH 解压到 `/var/www/demogo-preview/`，清理旧 assets
4. 验证结果：
   - ✅ 后端健康检查通过
   - ✅ `https://demogo.cn/` HTTP 200
   - ✅ `https://demogo.cn/login.html` HTTP 200
   - ✅ 新 CSS 文件 `main-BYpKLX17.css` 正常加载
   - ✅ 品牌色 `#22c55e` 通过 CSS 变量 `--accent` 驱动

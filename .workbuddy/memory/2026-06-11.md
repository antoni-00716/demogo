# 2026-06-11 DemoGo 上线前复盘与修复

## 第一阶段：运维修复
- 全面复盘 DemoGo v0.9.38 项目，检查了所有文档、代码、配置
- SSH 连接到阿里云服务器 (8.155.150.162, demogo.cn)
- 发现并修复了 4 个孤儿 Node 进程（释放约 140MB 内存）
- 删除了 1 个死掉的 Docker 容器

### 实施的上线前修复
1. MySQL 绑定地址从 `*:3306` 改为 `127.0.0.1`（安全风险修复）
2. MySQL X Protocol (33060) 绑定地址改为 `127.0.0.1`
3. 降低运行时并发实例数：`DEMOGO_RUNTIME_MAX_INSTANCES=1`，内存限制 `384m`
4. 设置 crontab 每日 3:00 备份数据（`/var/lib/demogo/data/` 和 `uploads/`）
5. 清理 journald 日志到 200MB
6. 更新 systemd 描述为 v0.9.38
7. 设置主机名为 `demogo`

## 第二阶段：代码级审查与修复
### 审查发现
- server.js 2630行，32个残留调试文件，36个工具文件
- 无优雅关闭（SIGTERM 只做了最小实现）
- 无用户磁盘配额

### 代码修复
1. 清理 28 个调试文件（server/src/ 中 server-fixed* server_fixed* server-debug* 等）
2. 清理 36 个调试工具文件（项目根目录的 fix-* check-* merge-* 等）
3. 增强 graceful shutdown：
   - 添加关闭标志防重复执行
   - HTTP server 关闭 + 排空请求
   - 停止所有运行中 Docker 容器
   - 关闭 Redis 队列 + MySQL 连接池
   - 添加 SIGQUIT 处理器
   - 添加 uncaughtException 和 unhandledRejection 处理器
4. 添加用户级磁盘配额：
   - Free: 200MB, Lite: 500MB, Pro: 2GB
   - 部署时自动计算并跟踪存储用量
   - 部署前检查存储配额

## 第三阶段：测试修复 + 发布部署
### 测试修复
1. 修复烟雾测试：`attachArchitectureToInspection` 中同步更新 `analysis.hostingMode`
2. 修复集成测试：`spawn("tar")` 改为 `tar` npm 包（Windows 兼容）
3. 集成测试和烟雾测试中 `spawn` import 被误删的问题已恢复

### 发布
1. 构建 8 个发布包（server/site/cli/mcp/ops/codex/claude + ops）
2. 清理 dist/ 中 28 个旧版本包
3. 从服务器恢复被误删的 `content-policy.html`
4. 完整部署 v0.9.38 到线上（SCP 上传 + server-deploy 脚本 + 健康验证）

### 服务器状态（最终）
- 服务健康：MySQL ✅ Redis ✅ Docker ✅
- 内存：652MB/1.8G 使用，1.2GB 可用
- 版本：v0.9.38（所有包统一）
- SSL 证书：2026-08-16（DigiCert）
- 单元测试：170/170 全部通过

## 第四阶段：安全加固与稳定修复（下午）
### 修复项目
1. **security-rules.js 扩展** — 新增 `.py .jar .rb .php .vbs .jsp .war` 到阻止列表（deploy-pipeline 侧）
2. **中间件去重** — 从 `routes/misc.js` 移除 4 个重复的中间件注册
3. **登录限流持久化** — 每 30 秒写入文件，重启恢复限流状态；暴露 `ready()` 供测试等待
4. **SMTP 重试** — 失败后指数退避（1s→2s→4s，最多 3 次）
5. **JSON 写锁** — 按文件路径加 per-file mutex，防止并发写覆盖

### 新增测试（6 个断言，总计 176/176）
- `tests/data-access.test.mjs` — 读写、并发锁、目录创建（4 个）
- `tests/login-rate-limiter-persist.test.mjs` — 持久化恢复、过期过滤（2 个）

### 监控运维
- 新增 `scripts/server-monitor.sh` — 每日 8:00 检查服务/SSL/磁盘/内存/Docker
- crontab 新增每日 3:00 备份 + 8:00 监控

## 第五阶段：竞品分析战略报告（产品战略团队协作）
- 启动产品战略团队（`product-competitive-analysis`），按 SOP 工作流 2（竞品分析）执行
- 调度 4 位成员并行工作：
  - **竞析（Compa）**：13 个竞品全景研究，发现 Tiiny.host 为最被忽视的直接竞品
  - **瑞思（Reese）**：用户视角竞品感知，发现三大 Tier 1 痛点（国内访问慢/Vercel 账单暴涨/最后一公里部署断裂）
  - **数析（Metric）×2**：两份市场数据报告，验证 Vibe Coding 部署市场 CAGR 36.79%，中国 AI 编程市场 CAGR 38.4%
- **关键上下文更新**：DemoGo 已有 CLI + MCP 协议能力，AI 编程工具可通过自然语言直接调用发布——这是未被充分认识的差异化壁垒
- 最终交付物：`deliverables/product-strategy/competitive-analysis-demogo-2026-06-11.md`
- **核心结论**：DemoGo 填补了「AI生成项目→快速分享试用」空白地带；P0 行动是推广 CLI/MCP 能力+深化插件生态

### 架构分析
- **server.js 拆解分析** 完成（`server/docs/server-vs-pipeline-analysis.md`，355 行）
  - 发现 16 个函数与 `deployment-pipeline-service.js` 重复
  - 7 个核心函数 + 8 个工具函数应迁出
  - 13 个函数已有共享库版本但 server.js 保留本地副本
  - 5 个任务排序：T01 替换 import → T02 合并辅助函数 → T03 消除注入 → T04 提取 restartRuntime → T05 删除重复

### server.js 拆分重构 — 全部 5 轮完成 ✅
- **T01** (9459df7): 替换 13 个本地函数为共享库 import（+241/−347 行）
- **T02** (69ecdd1): 合并 8 个工具函数到共享库（新建立 `lib/file-utils.js`）
- **T03** (862a0d6): 消除 `setFileHandlers` 惰性注入，pipeline 成为文件操作所有者
- **T04** (3f77d18): 提取 `restartDemoRuntime` 核心逻辑到 pipeline service
- **T05** (523fa95): 删除 15 个重复函数（+26/−1304 行），pipeline service 成为唯一事实源
- **结果**: server.js 从 ~2675 行缩减至 ~1370 行，净减 ~1300 行，176/176 测试通过
- **修复**: pipeline service 补上缺失的 `import logger`（之前会在运行时崩溃）

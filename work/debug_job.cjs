const path = require('path'); process.env.DEMOGO_DATA_DIR = '/var/lib/demogo/data'; const { readJson } = require('./src/lib/data-access.js'); const jobsFile = path.join('/var/lib/demogo/data', 'deployment-jobs.json'); async function main() { const jobs = await readJson(jobsFile, []); console.log('Jobs count:', jobs.length); const job = jobs.find(j => j.id === '496a14fc-61c3-4393-9d0d-a3f7c8e45536'); console.log('Found job:', job ? 'YES status='+job.status : 'NO'); } main().catch(e => console.error(e));
